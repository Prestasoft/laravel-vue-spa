name: flutter_votantes_app
description: Sistema de gestión de votantes PWA
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # HTTP & API
  dio: ^5.3.2
  dio_cache_interceptor: ^3.4.2
  dio_cache_interceptor_hive_store: ^3.2.1

  # State Management
  provider: ^6.0.5

  # Storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_secure_storage: ^9.0.0

  # UI & Design
  cupertino_icons: ^1.0.5
  google_fonts: ^5.1.0
  flutter_svg: ^2.0.7

  # Utils
  intl: ^0.18.1
  url_launcher: ^6.1.12
  share_plus: ^7.1.0

  # PWA
  pwa: ^0.1.10

  # Charts
  fl_chart: ^0.63.0

  # PDF/Excel Export
  pdf: ^3.10.4
  excel: ^2.1.0

  # Forms
  flutter_form_builder: ^9.1.1
  form_builder_validators: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.3.3
  hive_generator: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/